/* let myName = "Jesse Hampton"

function change (myName){
    let toUpperCase = myName.toUpperCase(myName) 
    // let toLowerCase = myName.toUpperCase(myName)
    console.log()
}
change (myName) */


var capitalizedAndLowerCase = "Hello"
​
function upper(b){
var uppeCase = b.toUpperCase()
var lowerCase = b.toLowerCase()
​
console.log(uppeCase.concat(lowerCase))
}
upper(capitalizedAndLowerCase)